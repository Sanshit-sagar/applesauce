import { styled } from '../../stitches.config'

const EditorHeader = styled('div', {
    display: 'flex',
    fd: 'inherit',
    jc: 'inherit',
    ai: 'center',
    borderBottom: ''
});

const Divider = styled('div', {

})